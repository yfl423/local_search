package tool;

// OP can be one of four binary operators: = (equality),
//! (inequality), > (greater than), and < (less than).
public enum Operator {
    EQUALITY, INEQUALITY, GREATER_THAN, LESS_THAN,
}
